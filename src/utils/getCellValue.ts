
/*import { Term } from "types/term";
import { Sheet } from "types/sheet";
import { Ref } from "types/Ref"; 


export function getCellValue(ref: Ref, sheet: Sheet): Term {
    const refKey = '$${ref.col}${ref.row}';
    const cellValue = sheet[refKey];
    if (cellValue === undefined) {
        throw new Error(`Cell ${refKey} is undefined`);
    }
    return value;
}
*/